## Lost WSL Password?

- Open a terminal
    - Type `wsl -d Ubuntu --user root`
    > Now you are logged in as root

    - Change your Android password:
        - `sudo passwd android`

        - Done!